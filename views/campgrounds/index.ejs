<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>campgrounds</title>
  </head>
  <body>
    <h1>All Campgrounds</h1>
    <% for (let campground of campgrounds) { %>
        <a href="<%= campground._id %>"><li><%= campground.title %></li></a>
      <% } %>
  </body>
</html>
